'use strict'

class RateController {

     /**
   * Create/save a new rate.
   * POST rate
   *
   * @param {object} ctx
   * @param {Request} ctx.request
   * @param {Response} ctx.response
   */
  async store ({ request, response, auth }) {
   
}

}

module.exports = RateController
